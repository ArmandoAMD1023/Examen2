document.addEventListener('DOMContentLoaded', function () {
    // Obtener la referencia al elemento donde se mostrará el listado de libros
    const librosLista = document.getElementById('libros-lista');

    // Realizar la petición (fetch) a la API de libros
  
});
